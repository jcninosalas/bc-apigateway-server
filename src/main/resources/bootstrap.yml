spring:
  application:
    name: bc-apigateway-server
  cloud:
    config:
      uri: https://config-server-pj2.mybluemix.net/
      username: ${SECURITY_USERNAME}
      password: ${SECURITY_PASSWORD}